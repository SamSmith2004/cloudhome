# === Secrets / Environment ===
.env
.env.*
!.env.example

# === Go ===
# Compiled binaries
# If building locally from the root directory (`go build ./api/cmd/server`)
/server
# If building locally from the 'api' directory (`cd api && go build ./cmd/server`)
api/server
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test results and coverage
*.out
*.test
/coverage.*
/profile.*

# Go workspace specific files (less common with modules)
/pkg/
/bin/

# Go dependencies (if using vendor)
/vendor/

# Go module download cache (shouldn't be in project dir anyway, but belt-and-suspenders)
GOPATH/pkg/mod/

# === Docker ===
# Potentially ignore build cache if stored locally (uncommon in project dir)
.docker/build-cache/

# === OS Generated Files ===
# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Windows
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
desktop.ini
*.stackdump
$RECYCLE.BIN/

# Linux / Misc
*~
*.swp
*.swo
*.bak
*.tmp

# === IDE / Editor ===
# VS Code
.vscode/
*.code-workspace

# GoLand / IntelliJ
.idea/
*.iml

# Vim
*.swp
*.swo

# === Logs ===
*.log
logs/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
